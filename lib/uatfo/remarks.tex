\subsection{Remarks}\label{c1fe077}

% \Remark{More about Tauber-Wiener functions}\label{d69d834}
%
% Let $g\in\href{e65fc0e}{\TW}$. Then for all $\lambda_i,\theta_i,c_i\in\R$ with
% $i=\iter1N$, we have that the mapping
% $$
%   x\mapsto\sum_{i=1}^Nc_ig(\lambda_ix+\theta_i)
% $$
%
% is dense in $C[a,b]$.
