#include <filesystem>
#include <fstream>
#include <functional>
#include <string>

#include "label.h"

using string = std::string;
namespace fs = std::filesystem;

int main(int argc, char *argv[]) {
    printf("HI\n");
    return 0;
}
